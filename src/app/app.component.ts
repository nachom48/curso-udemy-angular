import { Component, Input } from '@angular/core';

@Component({
  // el selector es el nombre q tiene el componenete, es lo q se inyecta
  //el template y templateUrl es el html de ese componenete
  selector: 'app-root',
  templateUrl: './app.component.html',
})
export class AppComponent {

}
